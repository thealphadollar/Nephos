---
# The file stores pre-processing configuration for nephos
# NOTE: If this file is edited, Nephos needs to be restarted to reflect changes
path_to_CCEx: 'ccextractor'  # absolute path to CCExtractor binary, or leave default for using system wide install
ccextractor_args: '-autoprogram'  # arguments to be used with CCExtractor
interval: 60  # minutes,
...